
<div class="new-cards container d-flex justify-content-center">
  <% @games.last(3).each do |game| %>
    <div class="card" style="width: 30rem;">
      <div class="image-container">
        <%= cl_image_tag game.photo.key, class: 'card-img-top' %>
      </div>
      <div class="card-body">
        <h5 class="card-title"><%= game.name %></h5>
        <p class="card-text">£<%= game.price %></p>
      </div>
      <%= link_to "Details", game_path(game), class: "btn btn-primary m-2" %>
    </div>
  <% end %>
</div>
